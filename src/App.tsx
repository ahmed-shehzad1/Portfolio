export default function App() {
  return (
    <main className="relative min-h-screen w-full overflow-hidden bg-void text-white bg-grain antialiased selection:bg-refract-cyan/20">
      {/* Future Canvas */}
      {/* Future Cursor */}
      {/* Future Sections */}
    </main>
  );
}